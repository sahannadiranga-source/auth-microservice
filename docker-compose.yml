version: '3.8'

services:
  authservice:
    build: ./AuthService
    ports:
      - "5001:80"

  profileservice:
    build: ./ProfileService
    ports:
      - "5002:80"

  apigateway:
    build: ./ApiGateway
    ports:
      - "5000:80"
    depends_on:
      - authservice
      - profileservice